spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  datasource:
    hikari:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/chat?currentSchema=chat
      username: jhjeon
      password: 1234
chat:
  netty:
    websocket:
      enabled: true
      port: 8090
      path: /ws/chat
